1.0版完成了以下几个功能：

1、用户登录
2、用户创建
3、cd命令

//1.1版在以上的基础上要完成
1、ls
2、remove  //未完成
3、puts
4、gets

服务器端用的是线程池，在任务队列中的任务结点上
含有对应的任务处理函数指针以及任务参数，便于每
个进程在取任务时直接可调用相应的处理函数完成任
务。在将任务结点插入队列时必须先执行epollDel(epfd,socketFd)，
以免在子线程工作时与主线程同时读取socketFd中的数据。
